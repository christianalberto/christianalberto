name: Update streak stats

on:
  schedule:
    - cron: "30 */6 * * *" # Every 6 hours, offset +30 min (00:30, 06:30, 12:30, 18:30 UTC)
  push:
    branches: ["main"]
  workflow_dispatch:

concurrency:
  group: christianalberto-profile-updates
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download Aurora Night streak card
        run: |
          mkdir -p profile
          curl --fail --location --retry 3 \
            "https://github-profiles-insights.vercel.app/api/insight?username=christianalberto&card=streak&theme=aurora_night&_t=$(date -u +%s)" \
            -o profile/streak.svg

      - name: Refresh README cache buster
        run: |
          VERSION=$(date -u +%Y%m%d%H%M%S)
          sed -i -E "s|(\./profile/streak\\.svg)(\\?v=[^\"' )]+)?|\1?v=${VERSION}|g" README.md

      - name: Commit streak stats
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add profile/streak.svg README.md

          if git diff --cached --quiet; then
            echo "No changes to commit."
            exit 0
          fi

          git commit -m "Update streak stats"
          git pull --rebase origin main

          for attempt in 1 2 3; do
            if git push; then
              exit 0
            fi
            git pull --rebase origin main
          done

          echo "Could not push after 3 attempts."
          exit 1
